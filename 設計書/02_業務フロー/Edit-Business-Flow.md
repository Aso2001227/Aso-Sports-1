```uml
@startuml
title: 会員情報編集
opt 会員情報編集
        ユーザー ->Webサーバー :更新する情報及び現在のパスワード入力\n(変更ない場合はそのまま)し、確定ボタンクリック
        Webサーバー -> DBサーバー : 現在のメールアドレス[セッションデータ]とパスワード参照
        alt 一致
                alt 新規パスワードが8文字以上で、英小文字、\n英大文字、数字を各1文字以上含む
                       Webサーバー -> DBサーバー : 更新許可
                       DBサーバー -> DBサーバー : 更新処理
                end
        end
        DBサーバー -> Webサーバー : 認証結果
end
@enduml
```
